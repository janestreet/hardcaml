(library
 ((name hardcaml)
  (public_name hardcaml)
  (preprocess (pps (ppx_jane -dont-check-doc-comments-attachment ppxlib.runner)))
  (libraries (base
              bigarray
              dynlink
              num
              stdio
              topological_sort
              unix))))

(jbuild_version 1)
