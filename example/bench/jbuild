(library
 ((name hardcaml_examples_bench)
  (libraries (hardcaml
              hardcaml_examples
              hardcaml_examples_tests))
  (preprocess (pps (ppx_jane ppxlib.runner)))))

(jbuild_version 1)
